---
paths: "**/*.md"
---

# Markdown編集ルール

プロジェクト内のすべてのMarkdownファイルに適用

- Markdown太字記法（`**text**`）を使用しない
- 文章末尾にコロン（`文章:`）を使用しない
- 絵文字を使用しない
- URLを`<>`で囲まない（コード表記または通常のリンク形式を使用）
- markdownlintルールを遵守（見出し前後の空行、リスト前後の空行等）
- 簡潔で明確な日本語を使用
