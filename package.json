{
	"name": "claude-code-sample",
	"version": "1.0.0",
	"private": true,
	"description": "AWS CDK (TypeScript) + Python Lambda project with Claude Code best practices",
	"scripts": {
		"format": "prettier --write .",
		"lint:md": "markdownlint-cli2 '**/*.md' --fix"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"markdownlint-cli": "^0.43.0",
		"markdownlint-cli2": "^0.17.1",
		"prettier": "^3.4.2"
	}
}
